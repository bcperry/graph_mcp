# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: simple-fastmcp-server
metadata:
  template: simple-fastmcp-server@0.0.1-beta
infra:
    provider: "bicep"
services:
  web:
    project: src
    language: py
    host: appservice